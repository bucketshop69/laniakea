import dotenv from 'dotenv';
import chalk from 'chalk';

dotenv.config();

console.log(chalk.blue('Starting AI agent simulation...'));

// Simple hello world script to verify setup
console.log(chalk.green('Client demo running successfully!'));